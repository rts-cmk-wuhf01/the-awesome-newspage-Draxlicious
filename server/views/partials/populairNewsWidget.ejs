


<h3>4 Most Popular News</h3>
<%latestNews.forEach(news => { %>
<% let now = new Date(news.dateTime); %>
    <!-- Single Popular Blog -->
    <div class="single-popular-post">
        <a href="#">
            <h6><span><%= news.number%>.</span><%= news.text%></h6>
        </a>
        <p><%= dateAndTime.format(now, 'MMMM DD, YYYY') %></p>
    </div>
<%}) %>